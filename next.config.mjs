/**
 * Next.js configuration for Captain Kiddo.
 *
 * NOTE: PWA-specific config (manifest, service worker, offline caching) is added
 * later in the `pwa01-manifest-and-service-worker` ticket. For now this is the
 * minimal base config so the project runs.
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
